---
title: Service Types
description: Payload detection and installation behavior for yeet services.
---

Yeet detects payload type and installs the service accordingly. This page is
the compact detection reference. For step-by-step usage, start with
[Payloads](/docs/payloads).

<CardLinks
  cards={[
    {
      title: "Containers",
      description: "Compose files, Dockerfiles, image refs, Python, and TypeScript.",
      href: "/docs/payloads/containers",
    },
    {
      title: "Binaries & Scripts",
      description: "Host systemd services from local executables and shebang scripts.",
      href: "/docs/payloads/binaries",
    },
    {
      title: "VMs",
      description: "Firecracker-backed Linux guests with yeet-managed image bundles.",
      href: "/docs/payloads/vms",
    },
    {
      title: "Cron Jobs",
      description: "Scheduled jobs installed as systemd timer units.",
      href: "/docs/payloads/cron-jobs",
    },
  ]}
/>

## Binary

- ELF or Mach-O binary.
- Catch installs it as a systemd service on the host.
- Use `yeet run <svc> ./bin/<svc>`.

## Script

- Shebang file (e.g., `#!/usr/bin/env bash`).
- Catch treats it like a binary and installs it as a systemd service.

## Docker Compose

- Any YAML with a top-level `services:` key.
- Catch installs it as a Docker Compose service.
- Use `--pull` on `yeet run` when you want to refresh images; otherwise `run`
  reuses existing images.
- See [Containers](/docs/payloads/containers).

## Docker image

- If the payload looks like an OCI image ref (e.g. `nginx:latest`,
  `ghcr.io/org/app:1.2.3`), catch pulls the image on the host.
- For local images, use `yeet docker push <svc> <image>:<tag> --run`.
- For Dockerfiles, use `yeet run <svc> ./Dockerfile` (builds for the host arch
  and pushes).
- See [Containers](/docs/payloads/containers).

## TypeScript

- `.ts` files are run via a Deno container.
- Catch runs it as a container-backed service.
- See [Containers](/docs/payloads/containers).

## Python

- `.py` files are run via a `uv` container image.
- Catch runs it as a container-backed service.
- See [Containers](/docs/payloads/containers).

## VM

- `vm://ubuntu/26.04` creates a long-lived Ubuntu VM service.
- `vm://nixos/26.05` creates a long-lived NixOS VM service.
- Requires a Linux catch host with KVM available.
- Uses yeet-owned VM image bundles published at
  `github.com/yeetrun/yeet-vm-images`.
- `yeet init` checks the host for KVM, TUN/TAP, and required VM tooling; on
  capable Debian/Ubuntu hosts it can prompt for missing VM tools, and
  `--install-vm-tools` answers yes for unattended setup.
- See [VMs](/docs/payloads/vms).

## Zstd

- Yeet detects and unpacks Zstd-compressed binaries.

## Cron

- `yeet cron` installs a cron-style job as a timer on the host.
- See [Cron Jobs](/docs/payloads/cron-jobs).

## Notes

- Yeet detects file type by extension or content.
- Architecture must match the target host; mismatches fail early.
- [ZFS](/docs/concepts/zfs) is storage backing for service roots and VM disks,
  not a service type.
