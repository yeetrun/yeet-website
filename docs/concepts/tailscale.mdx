---
title: Tailscale
description: How yeet uses Tailscale for host access and per-service networking.
---

Yeet uses Tailscale in two places:

- catch exposes host access through an embedded tsnet node.
- Services can get their own Tailscale identity with `--net=ts`.

You do not need the Tailscale client installed on your workstation for normal
yeet commands. The local Tailscale client is only needed for discovery commands
such as `yeet list-hosts`.

## First-time host setup

Catch must join your tailnet as a tagged node, usually `tag:catch`. Catch
rejects user-owned nodes.

Before you run `yeet init`, set up Tailscale in this order:

1. Add a yeet owner tag, such as `tag:yeet`.
2. Let that owner tag own the catch tag, usually `tag:catch`.
3. Let that owner tag own any service tags you plan to use later, such as
   `tag:app`.
4. Create a Tailscale OAuth client with the `auth_keys` scope.
5. Select the owner tag on the OAuth client.
6. Copy the `tskey-client-...` OAuth client secret.

Then run `yeet init root@<machine-host>` and paste that secret when prompted.
Validate the host with `yeet status`.

Minimal policy shape:

```jsonc
{
  "tagOwners": {
    "tag:yeet": ["autogroup:admin"],
    "tag:catch": ["tag:yeet"],
    "tag:app": ["tag:yeet"]
  },
  "grants": [
    { "src": ["autogroup:member"], "dst": ["tag:catch"], "ip": ["tcp:41548"] },
    { "src": ["tag:catch"], "dst": ["tag:catch"], "ip": ["tcp:41548"] }
  ]
}
```

The catch node joins as `tag:catch`. The OAuth client uses `tag:yeet` only so it
can mint auth keys for `tag:catch` and later service tags.

If you do not plan to use `--net=ts` for services, the OAuth client can assign
`tag:catch` directly. The owner-tag pattern is more flexible.

## Run init with the secret

Interactive init prompts for the OAuth client secret during first setup:

```bash
yeet init root@<machine-host>
```

For unattended setup:

```bash
yeet init --install-docker --ts-client-secret=<secret> root@<machine-host>
```

On VM-capable hosts:

```bash
yeet init --install-docker --install-vm-tools --ts-client-secret=<secret> root@<machine-host>
```

Advanced users can pass a preauthorized auth key for the catch node:

```bash
yeet init --install-docker --ts-auth-key=<key> root@<machine-host>
```

The secret or auth key belongs to catch itself. If the resulting catch node is
not tagged, catch refuses connections.

## Host naming

`yeet init root@<machine-host>` uses SSH. Normal commands use the catch tsnet
hostname:

```bash
CATCH_HOST=<catch-host> yeet status
yeet --host=<catch-host> status
yeet run <svc>@<catch-host> ./compose.yml
```

If another tailnet device already owns the requested catch hostname, Tailscale
may assign a suffixed name such as `catch-1`. Use that assigned catch host, or
remove the stale/conflicting Tailscale device and rerun `yeet init`.

## Service networking (`--net=ts`)

Use `--net=ts` when a service should get its own tailnet IP:

```bash
yeet run <svc> ./compose.yml --net=ts --ts-tags=tag:app
yeet ip <svc>
```

Use `svc,ts` when the service should keep service-network egress and also get a
Tailscale identity:

```bash
yeet run <svc> ./compose.yml --net=svc,ts --ts-tags=tag:app
```

Make sure the OAuth client saved during catch setup can assign every tag you
pass with `--ts-tags`.

Options:

- `--ts-tags=<tag>` sets service tags.
- `--ts-exit=<node>` uses a Tailscale exit node.
- `--ts-ver=<ver>` selects a Tailscale version for the service.
- `--ts-auth-key=<key>` passes a direct auth key for that service.

## Tailscale commands for a service

Run Tailscale commands for a service identity:

```bash
yeet tailscale <svc> -- status
yeet ts <svc> -- debug daemon-logs
```

Update the yeet-managed service Tailscale binary:

```bash
yeet ts <svc> update
yeet ts <svc> update --version=1.95.112
```

If you intentionally want Tailscale's own upstream `update` subcommand, pass it
through after `--`:

```bash
yeet ts <svc> -- update
```

## Tailscale Serve

Expose a service over HTTPS on your tailnet:

```bash
yeet ts <svc> serve --bg 3000
```

Serve requires tailnet HTTPS. With `--bg`, the Serve config persists across
reboots.

## Cleanup

When you remove a service, yeet attempts to delete that service's Tailscale
device from your tailnet.

## References

- https://tailscale.com/docs/features/access-control/auth-keys
- https://tailscale.com/docs/features/oauth-clients
- https://tailscale.com/docs/features/tags
- https://tailscale.com/docs/reference/syntax/policy-file
- https://tailscale.com/docs/reference/syntax/grants
- https://tailscale.com/kb/1242/tailscale-serve
